server_id = "mcp_student_records"
display_name = "Student Records (SIS)"
version = "1.1.0"
domain = "education"
sdk_version = "0.1.0"
risk_level = "high"
writes_allowed = "gated"
transports = ["stdio"]
credentials = []
governance_gates = []
environments = ["development", "staging", "production"]

[[tools]]
name = "find_students"
description = "Find students by SIS id, name, or grade level (FERPA-logged)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "get_student"
description = "Get a student's record (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "set_student_status"
description = "Set a student's enrollment status"
risk_class = "internal_write"
requires_approval = true
credential_bindings = []

[[tools]]
name = "add_accommodation"
description = "Add an IEP/504/ELL accommodation to a student"
risk_class = "internal_write"
requires_approval = true
credential_bindings = []

[[tools]]
name = "get_access_log"
description = "Get the FERPA access log (accounting of disclosures) for a student"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "create_course"
description = "Create a catalog course"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "list_courses"
description = "List catalog courses"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "create_section"
description = "Create a course section with grade-category weights"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "enroll"
description = "Enroll a student in a section"
risk_class = "internal_write"
requires_approval = true
credential_bindings = []

[[tools]]
name = "get_enrollments"
description = "List a student's enrollments (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "create_assignment"
description = "Create an assignment in a section"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "record_grade"
description = "Record a grade (with optional rubric) for a student"
risk_class = "internal_write"
requires_approval = true
credential_bindings = []

[[tools]]
name = "get_grades"
description = "Get all grades for a student (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "gradebook"
description = "Compute a student's weighted course grade for a section (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "record_attendance"
description = "Record an attendance entry for a student"
risk_class = "internal_write"
requires_approval = true
credential_bindings = []

[[tools]]
name = "attendance_summary"
description = "Get a student's attendance summary and rate (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "open_support_case"
description = "Open a student support case"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "get_support_case"
description = "Get a support case with notes"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "add_case_note"
description = "Add a note to a support case"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "set_case_status"
description = "Set a support case status"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "get_support_cases"
description = "List a student's support cases (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "add_guardian"
description = "Add a guardian to a student (consent flag for FERPA disclosures)"
risk_class = "internal_write"
requires_approval = true
credential_bindings = []

[[tools]]
name = "get_guardians"
description = "List a student's guardians (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "send_communication"
description = "Send a communication to a guardian (FERPA-gated on consent)"
risk_class = "external_write"
requires_approval = true
credential_bindings = []

[[tools]]
name = "get_communications"
description = "List communications about a student (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "get_transcript"
description = "Get a student's transcript and cumulative GPA (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "analytics"
description = "Learning-analytics standing: grade average, attendance, risk (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

# ── v1.1: competency / mastery ──────────────────────────────────────────────

[[tools]]
name = "add_competency"
description = "Add a learning competency/standard with optional prerequisites"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "list_competencies"
description = "List competencies, optionally by subject"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "record_mastery_evidence"
description = "Record a mastery evidence observation (updates mastery level)"
risk_class = "internal_write"
requires_approval = true
credential_bindings = []

[[tools]]
name = "get_mastery"
description = "Get a student's competency mastery (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "learning_path"
description = "Adaptive learning path: next competencies ready to learn (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

# ── v1.1: degree audit / standing / holds ───────────────────────────────────

[[tools]]
name = "add_grad_requirement"
description = "Add a graduation/program requirement"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "list_grad_requirements"
description = "List graduation requirements for a program"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "degree_audit"
description = "Earned vs. required credits with progress and on-track flag (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "academic_standing"
description = "Academic standing derived from GPA (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "place_hold"
description = "Place a registration/transcript/financial hold on a student"
risk_class = "internal_write"
requires_approval = true
credential_bindings = []

[[tools]]
name = "release_hold"
description = "Release a hold"
risk_class = "internal_write"
requires_approval = true
credential_bindings = []

[[tools]]
name = "get_holds"
description = "List holds on a student (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

# ── v1.1: early warning / interventions ─────────────────────────────────────

[[tools]]
name = "raise_flag"
description = "Raise an early-warning flag on a student"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "set_flag_status"
description = "Set an early-warning flag's status"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "get_flags"
description = "List early-warning flags on a student (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "evaluate_early_warning"
description = "Auto-evaluate grades/attendance/mastery and raise flags past threshold"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "assign_intervention"
description = "Assign an MTSS/RTI intervention (tier 1–3)"
risk_class = "internal_write"
requires_approval = true
credential_bindings = []

[[tools]]
name = "end_intervention"
description = "End an active intervention"
risk_class = "internal_write"
requires_approval = false
credential_bindings = []

[[tools]]
name = "get_interventions"
description = "List a student's interventions (FERPA read)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

